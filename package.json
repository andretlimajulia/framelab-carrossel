{
  "name": "framelab-desktop-server",
  "version": "1.0.0",
  "description": "FrameLab Desktop — servidor local de templates",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
